export async function GET() {
  return new Response("MCP is now handled in-process in the AI SDK.", { status: 200 });
}
export async function POST() {
  return new Response("MCP is now handled in-process in the AI SDK.", { status: 200 });
}
